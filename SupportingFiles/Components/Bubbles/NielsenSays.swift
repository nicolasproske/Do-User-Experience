import SwiftUI

struct NielsenSays: View {
    var body: some View {
        Text("👨🏻‍🦳 Jakob Nielsen says ...")
            .defaultFont(size: 16)
            .foregroundColor(.white)
    }
}
